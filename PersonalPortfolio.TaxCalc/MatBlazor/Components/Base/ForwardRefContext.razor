@namespace MatBlazor
@typeparam TRef

@ChildContent(_context)

@code
{
    private ForwardRef<TRef> _context = new();

    [Parameter]
    public RenderFragment<ForwardRef<TRef>> ChildContent { get; set; }
}